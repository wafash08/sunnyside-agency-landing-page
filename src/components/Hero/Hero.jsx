import styled from "styled-components";

const Hero = () => {
  return (
    <div>
      <Bakcground />
    </div>
  );
};

const Bakcground = styled.div`
  height: 100vh;
  background-color: blue;
`;

export default Hero;
